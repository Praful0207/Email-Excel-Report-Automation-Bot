# Email & Excel Report Automation Bot
**Author:** Praful Verma

## Overview
Streamlit app that reads a sales Excel file, computes summary statistics, displays charts, and sends a summary email.

## How to Run
1. Install requirements: `pip install -r requirements.txt`
2. Run: `streamlit run streamlit_app.py`
